// AUTO-GENERATED by scaffold-stacks. DO NOT EDIT.
// Re-run `stacksdapp generate` to update.

import { request } from '@stacks/connect';
import { fetchCallReadOnlyFunction, cvToValue, Cl, ClarityValue } from '@stacks/transactions';
import { scaffoldConfig } from '../scaffold.config';

let _deployments: Record<string, any> = { contracts: {} };
try { _deployments = require('./deployments.json'); } catch {}

function getContractId(name: string): { address: string; contractName: string } {
  const contractId: string = _deployments?.contracts?.[name]?.contract_id ?? '';
  if (!contractId) {
    console.warn(`[scaffold-stacks] "${name}" not deployed. Run stacksdapp deploy.`);
    return { address: '', contractName: name };
  }
  const dot = contractId.lastIndexOf('.');
  return { address: contractId.slice(0, dot), contractName: contractId.slice(dot + 1) };
}


// ── token ──────────────────────



export async function token_mint(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('token');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'mint',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function token_setTokenUri(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('token');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'set-token-uri',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function token_transfer(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('token');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'transfer',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}





export async function token_getBalance(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-balance',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function token_getDecimals(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-decimals',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function token_getName(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-name',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function token_getSymbol(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-symbol',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function token_getTokenUri(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-token-uri',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function token_getTotalSupply(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('token');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-total-supply',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}



// ── counter ──────────────────────



export async function counter_decrement(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('counter');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'decrement',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function counter_increment(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('counter');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'increment',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function counter_reset(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('counter');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'reset',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}





export async function counter_getCount(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('counter');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-count',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}



// ── nft ──────────────────────



export async function nft_mint(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('nft');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'mint',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function nft_setBaseUri(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('nft');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'set-base-uri',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}




export async function nft_transfer(
  functionArgs: ClarityValue[] = [],
  postConditions: any[] = [],
): Promise<any> {
  const { address, contractName } = getContractId('nft');
  if (!address) return undefined;
  // v8: request() accepts ClarityValue[] directly — no serialization needed
  return request('stx_callContract', {
    contract: `${address}.${contractName}`,
    functionName: 'transfer',
    functionArgs,
    postConditions,
    postConditionMode : 'allow',
    network: scaffoldConfig.targetNetwork,
  });
}





export async function nft_getLastTokenId(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('nft');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-last-token-id',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function nft_getOwner(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('nft');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-owner',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}




export async function nft_getTokenUri(
  functionArgs: ClarityValue[] = [],
  senderAddress?: string
): Promise<unknown> {
  const { address, contractName } = getContractId('nft');
  if (!address) return null;
  const result = await fetchCallReadOnlyFunction({
    contractAddress: address,
    contractName,
    functionName: 'get-token-uri',
    functionArgs,
    network: scaffoldConfig.targetNetwork,
    senderAddress: senderAddress ?? address,
  });
  return cvToValue(result);
}



